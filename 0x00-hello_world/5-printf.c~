#include <stdio.h>

/**
 * main - entry point
 * @void: no argumnet
 *
 * Return: Returns a value of 0
 */

int main (void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
